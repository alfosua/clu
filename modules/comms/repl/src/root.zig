/// comms/repl — Interactive REPL mode.
/// Minimal terminal I/O: prompt, streaming output, tool call display,
/// built-in commands, shell prefixes (!/ !!), thinking blocks, multi-line input.
pub const Repl = struct {};
