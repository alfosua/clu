/// comms/rpc — RPC server for IDE and TUI bridge integrations.
/// Transport: Unix domain socket (named pipe on Windows). Wire format: NDJSON.
/// Methods: create_session, send_message, destroy_session.
pub const RpcServer = struct {};
